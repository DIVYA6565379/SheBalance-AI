version: '3.9'
services:
  mongodb:
    image: mongo:6
    container_name: shebalance-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - shebalance-mongo-data:/data/db

volumes:
  shebalance-mongo-data:

